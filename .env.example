# Optional: OpenRouter API key. Only routes with observed zero pricing are admitted.
OPENROUTER_API_KEY=your_openrouter_api_key_here

# Optional: local SQLite control-plane path (defaults to ./harness-fleet.db)
HARNESS_FLEET_DB=harness-fleet.db
